/*use std::ptr::null;
use named_pip::PipeClient;
use std::ffi::OsStr;*/

pub fn editor_paste(message: &str) -> Result<bool, String> {
	//let osstr = OsStr::new(message);
	//let client = PipeClient::connect_ms(osstr, )
	unimplemented!();
}