type @person

impl @person {
    new: (age: @number) {
        return @person::{
            age
        }
    },

    birthday: (self) {
        self.age += 1
    }
}

let p = @person::new(20)
p.birthday()

$.print(p)

